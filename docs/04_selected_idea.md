# 탄소중립 토지이용 시뮬레이터 - 상세 기획서

> **경기 기후 바이브코딩 해커톤 2025**
>
> 전문가(개발) 트랙 참가 프로젝트

---

## 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [프로젝트명 및 브랜딩](#2-프로젝트명-및-브랜딩)
3. [기술 스택](#3-기술-스택-2025년-최신)
4. [상세 기능 명세](#4-상세-기능-명세)
5. [탄소 계산 알고리즘](#5-탄소-계산-알고리즘)
6. [화면 설계](#6-화면-설계)
7. [User Journey](#7-user-journey)
8. [데이터 흐름 및 아키텍처](#8-데이터-흐름-및-아키텍처)
9. [MVP 범위 정의](#9-mvp-범위-정의)
10. [발표 전략](#10-발표-전략)
11. [리스크 및 대응방안](#11-리스크-및-대응방안)
12. [참고 자료](#12-참고-자료)
13. [해커톤 전 준비 체크리스트](#13-해커톤-전-준비-체크리스트)

---

## 1. 프로젝트 개요

### 1.1 핵심 가치

> **"이 땅을 개발하면, 탄소는 얼마나 변할까?"**

개발/보전 계획에 따른 탄소 흡수/저장/배출 변화를 시뮬레이션하는 **의사결정 지원 도구**

### 1.2 해결하려는 문제

| 문제 | 현재 상황 | 해결 방안 |
|------|-----------|-----------|
| 탄소 영향 파악 불가 | 토지이용 변경 시 탄소 영향을 알 수 없음 | 실시간 시뮬레이션 제공 |
| 전문 용역 필요 | 환경영향평가에 비용/시간 소요 | 웹 기반 즉시 분석 |
| 시민 이해도 부족 | 개발사업의 환경 영향 불투명 | 직관적 시각화 |

### 1.3 타겟 사용자

| 사용자 | 니즈 | 제공 가치 |
|--------|------|-----------|
| **공무원** | 도시계획/개발허가 의사결정 | 정량적 탄소 영향 근거 |
| **개발사업자** | 환경영향평가 사전검토 | 빠른 사전 분석 |
| **시민** | 개발사업 환경 영향 이해 | 쉬운 시각화 |

### 1.4 평가 기준 대응

| 평가 기준 | 비중 | 대응 전략 |
|-----------|------|-----------|
| 기술 완성도 | 40% | 실제 동작하는 MVP, API 연동, 깔끔한 코드 |
| 정책 임팩트 | 30% | 도시계획/환경영향평가 실무 활용 가능성 |
| 혁신성 | 30% | 국내 최초 웹 기반 토지이용 탄소 시뮬레이터 |

---

## 2. 프로젝트명 및 브랜딩

### 2.1 프로젝트명

| 구분 | 이름 |
|------|------|
| **한글명** | **카본랜드** (CarbonLand) |
| **영문명** | CarbonLand - Carbon-Neutral Land Use Simulator |
| **축약형** | C-Land |

### 2.2 대안 이름

| 한글명 | 영문명 | 컨셉 |
|--------|--------|------|
| 탄소땅 | CarbonTTang | 직관적, 친근한 |
| 그린플롯 | GreenPlot | 토지 계획 강조 |
| 탄소미래지도 | Carbon Future Map | 미래지향적 |
| 땅의 숨 | Land Breath | 탄소=호흡 메타포 |

### 2.3 슬로건

**메인 슬로건**
> "이 땅을 개발하면, 탄소는 얼마나 변할까?"

**서브 메시지**
- 의사결정자용: *"데이터 기반 탄소중립 토지이용 의사결정 지원"*
- 시민용: *"당신의 선택이 지구의 숨을 바꿉니다"*
- 기술적: *"경기도 전역 10m 해상도 탄소 시뮬레이션"*

### 2.4 비주얼 아이덴티티

```
색상 팔레트
─────────────────────────────────────────────
주 색상 (탄소 흡수/보전)
  ██ #22c55e (Green 500)  - 긍정적 변화
  ██ #16a34a (Green 600)  - 강조

보조 색상 (탄소 배출/개발)
  ██ #f97316 (Orange 500) - 경고
  ██ #ef4444 (Red 500)    - 부정적 변화

중립 색상 (UI 베이스)
  ██ #1e293b (Slate 800)  - 텍스트
  ██ #f1f5f9 (Slate 100)  - 배경
  ██ #3b82f6 (Blue 500)   - 액션
─────────────────────────────────────────────
```

---

## 3. 기술 스택 (2025년 최신)

### 3.1 프론트엔드 코어

| 카테고리 | 기술 | 버전 | 선택 근거 |
|----------|------|------|-----------|
| 프레임워크 | **Next.js** | 15.x | App Router, Server Components |
| UI 라이브러리 | **React** | 19.x | Concurrent 기능, Suspense |
| 언어 | **TypeScript** | 5.x | 타입 안정성 |

### 3.2 지도 라이브러리: OpenLayers

**선택 근거:**
1. **WMS/WFS/WMTS 네이티브 지원** - 경기기후플랫폼 API 완벽 호환
2. **GIS 기능 풍부** - 벡터/래스터 레이어, 피처 편집, 공간 쿼리
3. **대용량 데이터 처리** - 10m 해상도 래스터 타일 효율적 렌더링

**대안 비교:**

| 라이브러리 | WMS/WFS | GIS 기능 | 학습곡선 | 판정 |
|-----------|---------|----------|----------|------|
| OpenLayers | ★★★★★ | ★★★★★ | ★★★ | **선택** |
| MapLibre GL | ★★★★ | ★★★ | ★★★★ | 대안 |
| Leaflet | ★★★★ | ★★★ | ★★★★★ | 백업 |

### 3.3 상태관리 및 데이터

| 카테고리 | 기술 | 선택 근거 |
|----------|------|-----------|
| 전역 상태 | **Zustand** | 경량, SSR 친화적, 간단한 API |
| 서버 상태 | **TanStack Query** | API 캐싱, 자동 갱신, 로딩/에러 상태 |
| 폼 상태 | **React Hook Form** | 폼 검증, 성능 최적화 |

### 3.4 UI 컴포넌트

| 카테고리 | 기술 | 선택 근거 |
|----------|------|-----------|
| 컴포넌트 | **shadcn/ui** | 커스터마이징 용이, Tailwind 기반 |
| 스타일링 | **Tailwind CSS 4** | CSS-first 구성, 빠른 개발 |
| 아이콘 | **Lucide React** | 경량, 일관된 디자인 |
| 차트 | **Recharts** | React 친화적, 선언적 API |

### 3.5 공간분석 라이브러리

| 라이브러리 | 용도 |
|-----------|------|
| **Turf.js** | 면적 계산, 버퍼, 교차 분석 |
| **proj4js** | 좌표 변환 (EPSG:3857 ↔ EPSG:4326) |

### 3.6 전체 기술 스택 다이어그램

```
┌──────────────────────────────────────────────────┐
│                  사용자 브라우저                    │
└──────────────────────────────────────────────────┘
                         │
┌──────────────────────────────────────────────────┐
│                   Next.js 15                      │
│  ┌────────────────────────────────────────────┐  │
│  │          React 19 + TypeScript             │  │
│  ├────────────────────────────────────────────┤  │
│  │  shadcn/ui  │  Recharts  │  React Hook Form│  │
│  ├────────────────────────────────────────────┤  │
│  │        OpenLayers + Turf.js                │  │
│  ├────────────────────────────────────────────┤  │
│  │     Zustand     │    TanStack Query        │  │
│  └────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────┘
                         │
          ┌──────────────┴──────────────┐
          │                             │
┌─────────┴─────────┐       ┌───────────┴───────────┐
│  경기기후플랫폼     │       │    Next.js API        │
│  WMS/WFS API      │       │    Routes (계산)      │
└───────────────────┘       └───────────────────────┘
```

---

## 4. 상세 기능 명세

### 4.1 기능 목록

| ID | 기능명 | 우선순위 | MVP |
|----|--------|----------|-----|
| F01 | 지도 기반 영역 선택 | P0 | ✅ |
| F02 | 현재 탄소 현황 조회 | P0 | ✅ |
| F03 | 토지이용 변경 시뮬레이션 | P0 | ✅ |
| F04 | 탄소 변화량 계산 | P0 | ✅ |
| F05 | 결과 시각화 대시보드 | P0 | ✅ |
| F06 | 비교 분석 (개발 vs 보전) | P1 | ✅ |
| F07 | 리포트 생성/다운로드 | P2 | ❌ |
| F08 | 시나리오 저장/불러오기 | P2 | ❌ |

### 4.2 F01: 지도 기반 영역 선택

**입력:** 사용자 마우스 클릭/드래그 (사각형 또는 다각형)

**처리 로직:**
```typescript
interface SelectedArea {
  geometry: GeoJSON.Polygon;
  areaM2: number;                    // 면적 (m²)
  areaHa: number;                    // 면적 (ha)
  centroid: [number, number];        // 중심점
  bbox: [number, number, number, number];  // 바운딩 박스
}

// Turf.js를 이용한 면적 계산
import * as turf from '@turf/turf';

function calculateArea(polygon: GeoJSON.Polygon): SelectedArea {
  const feature = turf.polygon(polygon.coordinates);
  const areaM2 = turf.area(feature);
  const centroid = turf.centroid(feature);
  const bbox = turf.bbox(feature);

  return {
    geometry: polygon,
    areaM2,
    areaHa: areaM2 / 10000,
    centroid: centroid.geometry.coordinates as [number, number],
    bbox: bbox as [number, number, number, number]
  };
}
```

**출력:**
- 선택 영역 지도 위 하이라이트
- 면적 정보 표시 (m², ha)

### 4.3 F02: 현재 탄소 현황 조회

**데이터 소스:**

| 레이어명 | API 레이어 ID | 단위 | 설명 |
|----------|---------------|------|------|
| 탄소흡수량 | spggcee:rst_npp | tC/ha/year | 순일차생산성(NPP) |
| 수목 탄소저장량 | spggcee:plnt_cbn_strgat_biotop | tC/ha | 지상부 바이오매스 |
| 토양 탄소저장량 | spggcee:rst_soil_cbn_strgat_32652 | tC/ha | 토양 탄소축적량 |
| 비오톱 유형 | spggcee:biotop_lclsf | 코드 | 토지이용 유형 |

**출력:**
```typescript
interface CarbonStatus {
  totalAbsorption: number;    // 연간 총 탄소흡수량 (tCO2/year)
  treeStorage: number;        // 수목 탄소저장량 (tC)
  soilStorage: number;        // 토양 탄소저장량 (tC)
  totalStorage: number;       // 총 탄소저장량 (tC)
  biotopTypes: BiotopBreakdown[];  // 비오톱 유형별 면적
  averageNPP: number;         // 평균 NPP (tC/ha/year)
}

interface BiotopBreakdown {
  typeCode: string;
  typeName: string;
  areaHa: number;
  percentage: number;
}
```

### 4.4 F03: 토지이용 변경 시뮬레이션

**변경 옵션:**

| 변경 유형 | 코드 | 설명 |
|-----------|------|------|
| 주거지 개발 | DEV_RES | 주거단지 조성 |
| 상업지 개발 | DEV_COM | 상업시설 조성 |
| 공업지 개발 | DEV_IND | 산업시설 조성 |
| 녹지 보전 | CON_GRN | 현재 녹지 유지 |
| 산림 복원 | RES_FOR | 산림 복원 |
| 도시숲 조성 | CRT_URB | 도시숲 신규 조성 |

**입력:**
```typescript
interface SimulationInput {
  currentStatus: CarbonStatus;
  changes: LandUseChange[];
  timeHorizon: number;   // 시뮬레이션 기간 (년)
}

interface LandUseChange {
  fromType: string;      // 현재 비오톱 유형
  toType: string;        // 변경 후 유형
  areaHa: number;        // 변경 면적
}
```

### 4.5 F05: 결과 시각화 대시보드

**출력 데이터:**
```typescript
interface DashboardData {
  // 현황 요약
  summary: {
    selectedAreaHa: number;
    currentStorage: number;      // 현재 탄소저장량 (tC)
    currentAbsorption: number;   // 현재 연간흡수량 (tC/year)
  };

  // 시뮬레이션 결과
  simulation: {
    projectedStorage: number;    // 예상 탄소저장량 (tC)
    projectedAbsorption: number; // 예상 연간흡수량 (tC/year)
    netChange: number;           // 순 변화량 (tC)
    changePercentage: number;    // 변화율 (%)
  };

  // 환산 지표
  equivalents: {
    trees: number;       // 30년생 소나무 환산 (그루)
    cars: number;        // 승용차 연간 배출량 환산 (대)
    households: number;  // 가구 연간 배출량 환산 (가구)
  };
}
```

---

## 5. 탄소 계산 알고리즘

### 5.1 탄소 흡수량 계산 (연간)

**공식:**
```
연간 탄소흡수량(tCO2/year) = NPP(tC/ha/year) × 면적(ha) × 44/12
```

- **NPP**: 순일차생산성 (경기기후플랫폼 `rst_npp` 데이터)
- **44/12**: 탄소(C)를 이산화탄소(CO2)로 변환하는 계수 (≈ 3.67)

### 5.2 탄소 저장량 계산

**공식:**
```
총 탄소저장량(tC) = 지상부 바이오매스 + 지하부 바이오매스 + 토양탄소
                 = 수목저장량 × (1 + 뿌리함량비) + 토양탄소저장량
```

### 5.3 토지이용 변경 탄소 계수

```typescript
// 토지이용 유형별 탄소 계수 (단위: tC/ha)
const CARBON_COEFFICIENTS = {
  FOREST: {        // 산림지
    storage: 150,   // 저장량 (tC/ha)
    absorption: 8,  // 연간 흡수량 (tC/ha/year)
    emission: 0     // 연간 배출량 (tC/ha/year)
  },
  GRASSLAND: {     // 초지/공원녹지
    storage: 50,
    absorption: 3,
    emission: 0
  },
  AGRICULTURAL: {  // 농경지
    storage: 30,
    absorption: 2,
    emission: 1
  },
  RESIDENTIAL: {   // 주거지
    storage: 10,
    absorption: 0.5,
    emission: 5
  },
  COMMERCIAL: {    // 상업지
    storage: 5,
    absorption: 0.2,
    emission: 10
  },
  INDUSTRIAL: {    // 공업지
    storage: 2,
    absorption: 0.1,
    emission: 20
  },
  WETLAND: {       // 습지
    storage: 200,
    absorption: 5,
    emission: 0
  }
};
```

### 5.4 탄소 변화량 계산 함수

```typescript
interface CarbonChangeResult {
  immediateEmission: number;      // 토지전환 시 즉시 배출 (tC)
  annualAbsorptionChange: number; // 연간 흡수량 변화 (tC/year)
  annualEmissionChange: number;   // 연간 배출량 변화 (tC/year)
  netAnnualChange: number;        // 순 연간 변화 (tC/year)
  cumulativeChange: number;       // 설정 기간 누적 변화 (tC)

  // 이해하기 쉬운 환산값
  equivalentTrees: number;        // 30년생 소나무 환산 (그루)
  equivalentCars: number;         // 승용차 연간 배출량 환산 (대)
  equivalentHouseholds: number;   // 가구 연간 배출량 환산 (가구)
}

function calculateCarbonChange(
  currentType: string,
  newType: string,
  areaHa: number,
  years: number = 30
): CarbonChangeResult {
  const current = CARBON_COEFFICIENTS[currentType];
  const future = CARBON_COEFFICIENTS[newType];

  // 즉시 배출량 (토지 전환 시 방출되는 저장 탄소의 50%)
  const immediateEmission = Math.max(0, current.storage - future.storage) * areaHa * 0.5;

  // 연간 흡수량 변화
  const annualAbsorptionChange = (future.absorption - current.absorption) * areaHa;

  // 연간 배출량 변화
  const annualEmissionChange = (future.emission - current.emission) * areaHa;

  // 순 연간 변화
  const netAnnualChange = annualAbsorptionChange - annualEmissionChange;

  // 누적 탄소 변화 (N년간)
  const cumulativeChange = netAnnualChange * years - immediateEmission;

  // 환산값 계산
  const tCO2 = Math.abs(cumulativeChange * 44/12);

  return {
    immediateEmission,
    annualAbsorptionChange,
    annualEmissionChange,
    netAnnualChange,
    cumulativeChange,
    equivalentTrees: Math.abs(cumulativeChange) / 0.5,      // 소나무 1그루 ≈ 0.5tC/30년
    equivalentCars: tCO2 / 4.6,                              // 승용차 연간 ≈ 4.6tCO2
    equivalentHouseholds: tCO2 / 2.5                         // 가구 연간 ≈ 2.5tCO2
  };
}
```

### 5.5 CO2 환산

```
CO2 배출량(tCO2) = 탄소량(tC) × 44/12 (≈ 3.67)
```

---

## 6. 화면 설계

### 6.1 전체 레이아웃

```
┌──────────────────────────────────────────────────────────────────┐
│  [로고] CarbonLand                              [정보] [가이드]   │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌───────────────────────────┐  ┌─────────────────────────────┐  │
│  │                           │  │      컨트롤 패널             │  │
│  │                           │  │  ┌───────────────────────┐  │  │
│  │                           │  │  │  레이어 컨트롤         │  │  │
│  │        지도 영역           │  │  │  [✓] 탄소흡수지도     │  │  │
│  │        (70% 너비)          │  │  │  [ ] 수목탄소저장     │  │  │
│  │                           │  │  │  [ ] 토양탄소저장     │  │  │
│  │  ┌─────────────────────┐  │  │  └───────────────────────┘  │  │
│  │  │ [□] [◇] [✕] [↺]   │  │  │                             │  │
│  │  │ 도구버튼             │  │  │  ┌───────────────────────┐  │  │
│  │  └─────────────────────┘  │  │  │  선택 영역 정보        │  │  │
│  │                           │  │  │  면적: XX ha           │  │  │
│  │  ┌─────────────────────┐  │  │  │  위치: XX시 XX구       │  │  │
│  │  │ 범례                 │  │  │  └───────────────────────┘  │  │
│  │  │ 높음 ████ 낮음      │  │  │                             │  │
│  │  └─────────────────────┘  │  │  [탄소 현황 분석]           │  │
│  └───────────────────────────┘  └─────────────────────────────┘  │
│                                                                   │
│  ┌───────────────────────────────────────────────────────────┐   │
│  │  탄소 현황                                                 │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐         │   │
│  │  │저장량    │ │흡수량    │ │배출량    │ │순수지    │         │   │
│  │  │1,500 tC │ │80 tC/yr │ │20 tC/yr │ │+60 tC/yr│         │   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘         │   │
│  └───────────────────────────────────────────────────────────┘   │
│                                                                   │
│  ┌───────────────────────────────────────────────────────────┐   │
│  │  시나리오 시뮬레이션                                        │   │
│  │                                                            │   │
│  │  토지이용 변경: [주거지 개발 ▼]   기간: [30년 ────●───]    │   │
│  │                                                            │   │
│  │  ┌────────────────┐  →  ┌────────────────┐                │   │
│  │  │ 현재           │     │ 변경 후         │                │   │
│  │  │ ████████████   │     │ ████████████   │                │   │
│  │  │ 저장: 1,500 tC │     │ 저장: 300 tC   │                │   │
│  │  │ 흡수: 80 tC/yr │     │ 흡수: 10 tC/yr │                │   │
│  │  └────────────────┘     └────────────────┘                │   │
│  │                                                            │   │
│  │  변화량: -1,200 tC (저장) | -70 tC/yr (흡수)               │   │
│  │  환산: 소나무 2,400그루 | 승용차 950대 연간 배출량          │   │
│  └───────────────────────────────────────────────────────────┘   │
│                                                                   │
├──────────────────────────────────────────────────────────────────┤
│  데이터 출처: 경기기후플랫폼 | 경기 기후 바이브코딩 해커톤 2025   │
└──────────────────────────────────────────────────────────────────┘
```

### 6.2 컴포넌트 구조

```
src/components/
├── Map/
│   ├── MapContainer.tsx       # OpenLayers 지도 래퍼
│   ├── DrawingTools.tsx       # 영역 선택 도구
│   ├── LayerControl.tsx       # 레이어 on/off 토글
│   └── Legend.tsx             # 색상 범례
├── Panel/
│   ├── StatusCard.tsx         # 탄소 현황 카드
│   ├── SimulationForm.tsx     # 시뮬레이션 입력 폼
│   └── SelectionInfo.tsx      # 선택 영역 정보
└── Result/
    ├── ResultCard.tsx         # 결과 카드
    ├── ComparisonChart.tsx    # 비교 막대 차트
    └── EquivalentIcons.tsx    # 환산 지표 아이콘
```

---

## 7. User Journey

### 7.1 사용자 여정 개요

CarbonLand의 세 가지 타겟 사용자별 서비스 이용 시나리오를 정의합니다.

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        CarbonLand 사용자 여정 맵                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   공무원 ────→ 심의 근거 확보 ────→ 정책 의사결정 지원                          │
│                                                                              │
│   개발사업자 ──→ 사전검토/비교 ────→ 환경 리스크 최소화                         │
│                                                                              │
│   시민 ────────→ 정보 확인 ────────→ 개발 모니터링/참여                        │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 7.2 Journey 1: 공무원 (도시계획/환경 담당자)

#### 페르소나

| 항목 | 내용 |
|------|------|
| **이름** | 김담당 (42세) |
| **직업** | 수원시청 도시계획과 주무관 |
| **목표** | 개발행위 허가 심의 시 탄소 영향에 대한 정량적 근거 확보 |
| **Pain Point** | 개발사업의 환경 영향 판단 근거 부족, 전문 용역 없이 탄소 영향 파악 불가 |
| **기대 가치** | 빠르고 신뢰할 수 있는 탄소 영향 분석 데이터 |

#### 상세 Journey

```
시간 ─────────────────────────────────────────────────────────────────────────→

[1. 인지]      [2. 접속]      [3. 탐색]      [4. 분석]      [5. 활용]
    │              │              │              │              │
    ▼              ▼              ▼              ▼              ▼
┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐
│ 심의 대상 │  │ 서비스  │  │ 해당 지역 │  │ 시나리오 │  │ 심의 자료 │
│ 개발사업 │→│ 접속    │→│ 검색/선택 │→│ 시뮬레이션│→│ 작성     │
│ 접수     │  │         │  │         │  │         │  │         │
└─────────┘  └─────────┘  └─────────┘  └─────────┘  └─────────┘

감정  ───────────────────────────────────────────────────────────────────────
      😟 걱정        😐 탐색       🤔 집중       😊 만족       😄 안심
      (근거 없음)    (학습 중)     (분석 중)     (데이터 확보)  (업무 완료)
```

#### 단계별 상세

| 단계 | 사용자 행동 | 시스템 응답 | 화면 |
|------|------------|------------|------|
| **1. 인지** | 개발행위 허가 신청서 검토 중 환경 영향 검토 필요성 인지 | - | - |
| **2. 접속** | CarbonLand 접속, 서비스 가이드 확인 | 랜딩 페이지, 사용 가이드 표시 | 메인 |
| **3. 탐색** | 주소 검색 또는 지도에서 심의 대상 부지 선택 | 선택 영역 하이라이트, 면적 표시 | 지도 |
| **4. 분석** | "주거지 개발" 시나리오 선택 후 시뮬레이션 실행 | 현재 탄소 현황 → 변경 후 예측 → 변화량 표시 | 대시보드 |
| **5. 활용** | 결과 화면 캡처/인쇄하여 심의 자료에 첨부 | 결과 요약 카드, 인쇄용 뷰 제공 | 결과 |

#### 핵심 터치포인트

```
┌────────────────────────────────────────────────────────────────┐
│  [검색창]  →  [영역선택]  →  [시나리오 드롭다운]  →  [결과 카드]  │
└────────────────────────────────────────────────────────────────┘
     ↓              ↓                  ↓                  ↓
  "수원시         10.5 ha           "주거지 개발"       "-1,200 tC
   영통구"        선택됨             선택               30년 누적"
```

---

### 7.3 Journey 2: 개발사업자

#### 페르소나

| 항목 | 내용 |
|------|------|
| **이름** | 박PM (38세) |
| **직업** | 중견 건설사 프로젝트 매니저 |
| **목표** | 사업 후보지 탄소 영향 사전검토로 환경 리스크 예측 |
| **Pain Point** | 환경영향평가 전 단계에서 탄소 영향 가늠 불가 |
| **기대 가치** | 여러 후보지 빠른 비교, 최적 입지 선정 근거 |

#### 상세 Journey

```
시간 ─────────────────────────────────────────────────────────────────────────→

[1. 후보지 탐색]   [2. 개별 분석]   [3. 비교 분석]   [4. 의사결정]
       │                │                │                │
       ▼                ▼                ▼                ▼
  ┌─────────┐      ┌─────────┐      ┌─────────┐      ┌─────────┐
  │ 3개 후보지│  →  │ A부지   │  →  │ A vs B  │  →  │ B부지   │
  │ 목록 확보 │      │ 시뮬레이션│      │ vs C 비교│      │ 최종 선정│
  └─────────┘      └─────────┘      └─────────┘      └─────────┘

감정  ───────────────────────────────────────────────────────────────────────
      🤔 고민         😐 작업        💡 인사이트      ✅ 확신
      (뭘 봐야 하지)   (데이터 수집)  (비교 결과)     (근거 확보)
```

#### 단계별 상세

| 단계 | 사용자 행동 | 시스템 응답 | 주요 기능 |
|------|------------|------------|----------|
| **1. 후보지 탐색** | 3개 후보지 각각 지도에서 확인 | 각 부지 위치 표시, 현재 비오톱 정보 | F01 영역선택 |
| **2. 개별 분석** | 각 후보지별로 "상업지 개발" 시뮬레이션 | 현재 탄소저장량, 개발 후 예상 변화량 | F02, F03, F04 |
| **3. 비교 분석** | 3개 결과 나란히 비교 | 부지별 탄소 영향 비교표/차트 | F06 비교분석 |
| **4. 의사결정** | 탄소 영향 최소 부지를 우선 검토 대상으로 선정 | 결과 저장/공유 기능 | 결과 |

#### 핵심 터치포인트

```
┌──────────────────────────────────────────────────────────────────────────┐
│                          부지별 탄소 영향 비교                              │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│   후보지 A (광교)      후보지 B (영통)      후보지 C (동탄)                  │
│   ─────────────       ─────────────       ─────────────                  │
│   현재: 800 tC        현재: 1,200 tC      현재: 400 tC                    │
│   변화: -600 tC       변화: -900 tC       변화: -300 tC ⭐                 │
│                                                                           │
│   [탄소 영향 최소: 후보지 C]                                               │
│                                                                           │
└──────────────────────────────────────────────────────────────────────────┘
```

---

### 7.4 Journey 3: 시민 (환경 관심 시민)

#### 페르소나

| 항목 | 내용 |
|------|------|
| **이름** | 이주민 (35세) |
| **직업** | 지역 환경단체 활동가 |
| **목표** | 내 지역 개발사업의 환경 영향 모니터링 및 주민 공유 |
| **Pain Point** | 개발사업 환경 영향 정보 접근 어려움, 전문용어 이해 곤란 |
| **기대 가치** | 쉽게 이해할 수 있는 시각화, 공유 가능한 데이터 |

#### 상세 Journey

```
시간 ─────────────────────────────────────────────────────────────────────────→

[1. 뉴스 인지]   [2. 정보 탐색]   [3. 직접 분석]   [4. 공유/활용]
      │               │               │               │
      ▼               ▼               ▼               ▼
 ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐
 │ "광교에  │ →  │ 해당 지역│ →  │ 개발 시  │ →  │ SNS/    │
 │ 대규모  │    │ CarbonLand│   │ 탄소 영향│    │ 주민모임 │
 │ 개발 예정"│   │ 에서 확인│    │ 시뮬레이션│    │ 공유    │
 └─────────┘    └─────────┘    └─────────┘    └─────────┘

감정  ───────────────────────────────────────────────────────────────────────
      😠 우려         🔍 탐구        😲 놀람         📢 참여
      (환경 걱정)     (알고 싶다)    (이 정도구나)   (알려야겠다)
```

#### 단계별 상세

| 단계 | 사용자 행동 | 시스템 응답 | 주요 화면 |
|------|------------|------------|----------|
| **1. 뉴스 인지** | 지역 뉴스에서 "○○ 택지개발 예정" 기사 확인 | - | - |
| **2. 정보 탐색** | CarbonLand 접속, 해당 지역 검색 | 현재 녹지 현황, 탄소 저장량 표시 | 지도 |
| **3. 직접 분석** | "주거단지 개발" 시나리오로 시뮬레이션 | 예상 탄소 배출량, "소나무 9,000그루 손실" 환산 | 대시보드 |
| **4. 공유/활용** | 결과 화면 캡처, SNS/주민모임에 공유 | 공유용 이미지, 핵심 수치 강조 | 결과 |

#### 핵심 터치포인트

```
┌──────────────────────────────────────────────────────────────────────────┐
│                     🌳 이해하기 쉬운 환산 지표                              │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│   이 녹지가 개발되면...                                                    │
│                                                                           │
│   🌲 소나무 9,000그루 사라지는 효과                                         │
│                                                                           │
│   🚗 승용차 950대가 1년간 배출하는 탄소                                     │
│                                                                           │
│   🏠 380가구가 1년간 배출하는 탄소                                          │
│                                                                           │
│                            [📤 공유하기]                                   │
│                                                                           │
└──────────────────────────────────────────────────────────────────────────┘
```

---

### 7.5 통합 User Flow

```
                                    ┌─────────────┐
                                    │   시작      │
                                    └──────┬──────┘
                                           │
                                    ┌──────▼──────┐
                                    │  랜딩 페이지  │
                                    │  (서비스 소개)│
                                    └──────┬──────┘
                                           │
                              ┌────────────┼────────────┐
                              │            │            │
                       ┌──────▼──────┐    ▼     ┌──────▼──────┐
                       │  주소 검색   │   또는   │  지도에서   │
                       │  (텍스트)    │         │  직접 선택   │
                       └──────┬──────┘         └──────┬──────┘
                              │                       │
                              └───────────┬───────────┘
                                          │
                                   ┌──────▼──────┐
                                   │  영역 선택   │
                                   │  (사각형/폴리곤)│
                                   └──────┬──────┘
                                          │
                                   ┌──────▼──────┐
                                   │  탄소 현황   │
                                   │  자동 조회   │
                                   └──────┬──────┘
                                          │
                                   ┌──────▼──────┐
                                   │ 시나리오 선택 │
                                   │ (토지이용 변경)│
                                   └──────┬──────┘
                                          │
                                   ┌──────▼──────┐
                                   │  시뮬레이션  │
                                   │  결과 표시   │
                                   └──────┬──────┘
                                          │
                              ┌───────────┼───────────┐
                              │           │           │
                       ┌──────▼──────┐   ▼    ┌──────▼──────┐
                       │  다른 시나리오│  또는  │   새 영역   │
                       │  비교        │        │   선택      │
                       └─────────────┘        └─────────────┘
```

---

### 7.6 Pain Points & Solutions

| 사용자 | Pain Point | CarbonLand 솔루션 |
|--------|-----------|------------------|
| **공무원** | 전문 용역 없이 탄소 영향 파악 불가 | 웹에서 즉시 시뮬레이션, 심의 근거 자료 생성 |
| **공무원** | 개발 vs 보전 비교 근거 부재 | 시나리오별 비교 분석 기능 제공 |
| **개발사업자** | 환경영향평가 전 리스크 예측 불가 | 사전검토용 빠른 탄소 영향 분석 |
| **개발사업자** | 여러 후보지 비교 어려움 | 다중 부지 비교 분석 기능 |
| **시민** | 환경 정보 접근성 낮음 | 누구나 무료로 이용 가능한 웹 서비스 |
| **시민** | 전문용어 이해 어려움 | 소나무/승용차/가구 등 쉬운 환산 지표 |

---

## 8. 데이터 흐름 및 아키텍처

### 8.1 Zustand 스토어 구조

```typescript
// stores/mapStore.ts
interface MapState {
  viewport: {
    center: [number, number];
    zoom: number;
  };
  layers: {
    [layerId: string]: {
      visible: boolean;
      opacity: number;
    };
  };
  selectedArea: SelectedArea | null;

  // Actions
  setViewport: (viewport: MapState['viewport']) => void;
  toggleLayer: (layerId: string) => void;
  setSelectedArea: (area: SelectedArea | null) => void;
  clearSelection: () => void;
}

// stores/carbonStore.ts
interface CarbonState {
  currentStatus: CarbonStatus | null;
  simulationInput: SimulationInput | null;
  simulationResult: CarbonChangeResult | null;
  comparisonResult: ScenarioComparison | null;

  // Actions
  setCurrentStatus: (status: CarbonStatus) => void;
  runSimulation: () => void;
  reset: () => void;
}
```

### 8.2 API 라우트 구조

```
/api
├── /carbon
│   ├── /status      POST  - 선택 영역 탄소 현황 조회
│   ├── /simulate    POST  - 시뮬레이션 실행
│   └── /compare     POST  - 시나리오 비교
└── /layers
    └── /list        GET   - 사용 가능한 레이어 목록
```

### 8.3 경기기후플랫폼 API 연동

```typescript
// lib/climateApi.ts
const API_BASE = 'https://climate.gg.go.kr/ols/api/geoserver';

// WMS GetMap 요청
export function getWMSUrl(
  layerName: string,
  bbox: number[],
  width: number,
  height: number,
  apiKey: string
) {
  const params = new URLSearchParams({
    apiKey,
    SERVICE: 'WMS',
    VERSION: '1.3.0',
    REQUEST: 'GetMap',
    LAYERS: layerName,
    CRS: 'EPSG:3857',
    BBOX: bbox.join(','),
    WIDTH: width.toString(),
    HEIGHT: height.toString(),
    FORMAT: 'image/png',
    TRANSPARENT: 'TRUE',
  });

  return `${API_BASE}/wms?${params}`;
}

// WFS GetFeature 요청
export async function getFeatures(
  layerName: string,
  bbox: number[],
  apiKey: string
): Promise<GeoJSON.FeatureCollection> {
  const params = new URLSearchParams({
    apiKey,
    SERVICE: 'WFS',
    VERSION: '1.1.0',
    REQUEST: 'GetFeature',
    TYPENAMES: layerName,
    OUTPUTFORMAT: 'application/json',
    BBOX: [...bbox, 'EPSG:3857'].join(','),
  });

  const response = await fetch(`${API_BASE}/wfs?${params}`);
  return response.json();
}
```

### 8.4 지도 레이어 설정

```typescript
const mapLayers = [
  {
    id: 'npp',
    name: '탄소흡수지도',
    type: 'WMS',
    layerName: 'spggcee:rst_npp',
    visible: true,
    opacity: 0.7
  },
  {
    id: 'tree_carbon',
    name: '수목탄소저장',
    type: 'WMS',
    layerName: 'spggcee:plnt_cbn_strgat_biotop',
    visible: false
  },
  {
    id: 'soil_carbon',
    name: '토양탄소저장',
    type: 'WMTS',
    layerName: 'spggcee:rst_soil_cbn_strgat_32652',
    visible: false
  },
  {
    id: 'biotop',
    name: '비오톱유형도',
    type: 'WMS',
    layerName: 'spggcee:biotop_lclsf',
    visible: false
  }
];
```

---

## 9. MVP 범위 정의

### 9.1 해커톤 당일 타임라인 (6시간)

| 시간 | 구간 | 작업 내용 | 산출물 |
|------|------|-----------|--------|
| 09:00-09:30 | 0.5h | 환경 설정, 프로젝트 초기화 | Next.js 프로젝트 |
| 09:30-10:30 | 1h | 기본 레이아웃, 지도 연동 | 지도 표시 |
| 10:30-11:30 | 1h | 경기기후플랫폼 API 연동 | 레이어 표시 |
| 11:30-12:30 | 1h | 영역 선택 기능 | 폴리곤 선택 |
| 12:30-13:00 | 0.5h | 점심 식사 | - |
| 13:00-14:00 | 1h | 탄소 현황 조회 | 현황 카드 |
| 14:00-15:00 | 1h | 시뮬레이션 로직 | 계산 결과 |
| 15:00-15:30 | 0.5h | 결과 시각화 | 차트/대시보드 |
| 15:30-16:00 | 0.5h | 버그 수정, 발표 준비 | 최종 데모 |

### 9.2 MVP 기능 체크리스트

| 기능 | 필수 | 세부 사항 |
|------|:----:|-----------|
| 지도 표시 | ✅ | OpenLayers + 배경지도 |
| 탄소흡수지도 레이어 | ✅ | WMS 레이어 표시 |
| 영역 선택 (사각형) | ✅ | 드래그로 영역 선택 |
| 면적 계산 | ✅ | Turf.js area() |
| 현재 탄소 현황 표시 | ✅ | 기본 통계 카드 |
| 토지이용 변경 선택 | ✅ | 드롭다운 2-3개 옵션 |
| 탄소 변화량 계산 | ✅ | 기본 공식 적용 |
| 결과 숫자 표시 | ✅ | 변화량, 환산값 |
| 간단한 비교 | ⚪ | 개발 vs 보전 수치 |
| 막대 차트 | ⚪ | Recharts 기본 차트 |

### 9.3 MVP 디렉토리 구조

```
src/
├── app/
│   ├── page.tsx              # 메인 페이지 (단일 페이지)
│   ├── layout.tsx            # 레이아웃
│   └── api/
│       └── carbon/
│           └── route.ts      # API 라우트 (통합)
├── components/
│   ├── Map/
│   │   ├── MapContainer.tsx  # 지도 컨테이너
│   │   └── DrawTool.tsx      # 영역 선택 도구
│   ├── Panel/
│   │   ├── StatusCard.tsx    # 현황 표시 카드
│   │   └── SimulationForm.tsx # 시뮬레이션 폼
│   └── Result/
│       ├── ResultCard.tsx    # 결과 카드
│       └── SimpleChart.tsx   # 간단한 차트
├── lib/
│   ├── climateApi.ts         # API 연동
│   ├── carbonCalc.ts         # 탄소 계산 로직
│   └── constants.ts          # 탄소 계수 상수
└── stores/
    └── store.ts              # Zustand 스토어 (통합)
```

### 9.4 향후 확장 기능 (Post-MVP)

| 우선순위 | 기능 | 설명 |
|----------|------|------|
| P1 | 다각형 선택 | 자유로운 형태 영역 선택 |
| P1 | 시계열 차트 | 30년 탄소 변화 추이 |
| P1 | 비오톱 연계 | 비오톱 유형별 세부 분석 |
| P2 | PDF 리포트 | 전문적인 보고서 생성 |
| P2 | 시나리오 저장 | 로컬/클라우드 저장 |
| P3 | 건물 배출량 연계 | 개발 후 건물 배출 예측 |
| P3 | AI 추천 | 최적 토지이용 제안 |

---

## 10. 발표 전략

### 10.1 5분 발표 구조

| 섹션 | 시간 | 내용 |
|------|------|------|
| Hook | 30초 | 문제 제기 |
| 문제 정의 | 30초 | 현재의 문제점 |
| 솔루션 소개 | 30초 | CarbonLand 소개 |
| **데모 시연** | **120초** | 실제 사용 시연 |
| 기술적 차별점 | 30초 | 기술 스택/알고리즘 |
| 정책 임팩트 | 30초 | 활용 방안/가치 |
| 마무리 | 30초 | 요약 및 CTA |

### 10.2 발표 스크립트

**[Hook - 30초]**
> "여러분, 경기도에서 10만 제곱미터 녹지를 개발하면 탄소는 얼마나 변할까요?
> 정답은... 아무도 모릅니다. 이를 쉽게 계산할 도구가 없기 때문입니다."

**[문제 정의 - 30초]**
> "현재 토지이용 의사결정에서 탄소 영향은 별도의 전문 용역을 통해야만 파악할 수 있습니다.
> 공무원, 도시계획가, 시민 누구도 개발이 탄소에 미치는 영향을 직관적으로 알 수 없습니다."

**[솔루션 소개 - 30초]**
> "CarbonLand는 이 문제를 해결합니다.
> 경기기후플랫폼의 고해상도 탄소 데이터를 활용해 누구나 3클릭으로 토지이용 변화에 따른 탄소 영향을 시뮬레이션할 수 있습니다."

**[데모 시연 - 120초]**
> 1. 지도에서 분석할 영역 선택 (수원시 인근 녹지 10ha)
> 2. 현재 탄소 현황 확인 (저장량 1,500톤, 흡수량 80톤/년)
> 3. 시나리오 비교 (주거지 개발 vs 산림 보전)
> 4. 결과 확인 (차이 = 소나무 9,000그루)

**[기술적 차별점 - 30초]**
> "CarbonLand는 경기기후플랫폼의 10m 해상도 탄소흡수지도, 토양탄소저장지도, 비오톱 데이터를 실시간으로 분석합니다.
> IPCC 가이드라인 기반의 탄소 계산 알고리즘으로 과학적으로 신뢰할 수 있는 결과를 제공합니다."

**[정책 임팩트 - 30초]**
> "CarbonLand가 있으면:
> - 도시계획 심의 시 탄소 영향 평가가 가능합니다.
> - 개발행위 허가 시 탄소 기준을 적용할 수 있습니다.
> - 시민들이 개발 사업의 환경 영향을 이해할 수 있습니다."

**[마무리 - 30초]**
> "이 땅을 개발하면, 탄소는 얼마나 변할까?
> CarbonLand가 답합니다.
> 감사합니다."

### 10.3 데모 시나리오

**메인 시나리오:**
1. 수원시 인근 녹지 선택
2. 현재 탄소 현황 조회
3. 주거지 개발 시나리오 선택
4. 결과 확인 및 환산값 설명

**백업 시나리오 (API 장애 시):**
- 사전 녹화된 데모 영상 (30초)
- 정적 스크린샷 슬라이드

---

## 11. 리스크 및 대응방안

### 11.1 리스크 매트릭스

| 리스크 | 발생확률 | 영향도 | 대응방안 |
|--------|:--------:|:------:|----------|
| API 연동 실패 | 중 | 상 | Mock 데이터 준비 |
| 시간 부족 | 상 | 상 | 범위 축소 전략 |
| 탄소 계산 오류 | 중 | 중 | 단순화 공식 사용 |
| 지도 렌더링 이슈 | 하 | 상 | Leaflet 백업 |
| 발표 장비 문제 | 하 | 상 | 백업 영상 준비 |

### 11.2 API 연동 실패 대응

**Mock 데이터 준비:**
```typescript
// lib/mockData.ts
export const mockCarbonStatus: CarbonStatus = {
  totalAbsorption: 80,      // tCO2/year
  treeStorage: 1200,        // tC
  soilStorage: 300,         // tC
  totalStorage: 1500,       // tC
  averageNPP: 8,            // tC/ha/year
  biotopTypes: [
    { typeCode: 'F01', typeName: '산림지', areaHa: 7, percentage: 70 },
    { typeCode: 'G01', typeName: '초지', areaHa: 3, percentage: 30 }
  ]
};

// API 호출 시 fallback
async function fetchCarbonStatus(area: SelectedArea) {
  try {
    const response = await fetch('/api/carbon/status', {...});
    if (!response.ok) throw new Error('API Error');
    return response.json();
  } catch (error) {
    console.warn('API 실패, Mock 데이터 사용');
    return generateMockData(area);
  }
}
```

### 11.3 시간 부족 대응 (점진적 축소)

| 버전 | 가용 시간 | 포함 기능 |
|------|-----------|-----------|
| Full | 6시간 | 지도 + API + 시뮬레이션 + 비교 + 차트 |
| 축소 v1 | 4시간 | 지도 + Mock데이터 + 시뮬레이션 |
| 축소 v2 | 2시간 | 정적 지도 + 시뮬레이션 폼 + 결과 |
| 최소 | 1시간 | 슬라이드형 프로토타입 |

---

## 12. 참고 자료

### 12.1 경기기후플랫폼
- 메인: https://climate.gg.go.kr/ips
- Open&Share: https://climate.gg.go.kr/ols
- API 문서: https://climate.gg.go.kr/ols/api

### 12.2 탄소 계산 관련
- 산림청 탄소흡수원 통계
- IPCC LULUCF 가이드라인
- 국가 온실가스 인벤토리

### 12.3 기술 스택
- Next.js: https://nextjs.org/
- OpenLayers: https://openlayers.org/
- Zustand: https://zustand-demo.pmnd.rs/
- shadcn/ui: https://ui.shadcn.com/
- Turf.js: https://turfjs.org/
- Recharts: https://recharts.org/

---

## 13. 해커톤 전 준비 체크리스트

### D-3 (12월 10일)
- [ ] 경기기후플랫폼 API 키 발급 신청
- [ ] 개발 환경 구축 (Node.js, pnpm)
- [ ] GitHub 저장소 생성

### D-2 (12월 11일)
- [ ] Next.js 프로젝트 초기화
- [ ] 기본 UI 컴포넌트 설정 (shadcn/ui)
- [ ] OpenLayers 연동 테스트
- [ ] API 키 수신 확인

### D-1 (12월 12일)
- [ ] 경기기후플랫폼 API 연동 테스트
- [ ] Mock 데이터 준비
- [ ] 탄소 계수 상수 입력
- [ ] 발표 자료 초안 작성
- [ ] 데모 시나리오 영역 좌표 저장

### D-Day (12월 13일)
- [ ] API 상태 확인 (아침)
- [ ] 코드 최종 백업
- [ ] 발표 리허설

---

*문서 버전: 2.1*
*최종 수정: 2025-12-10*
*추가: User Journey (섹션 7)*
*작성: 경기 기후 바이브코딩 해커톤 2025 참가팀*
